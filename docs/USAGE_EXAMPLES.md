# 使用示例

请替换为你自己项目的实际运行方式和输出。

## CLI 示例

```bash
python -m final_project.cli add "完成 Python 大作业" --category 学习 --duration 120 --note "实现 CLI 和 Streamlit"
python -m final_project.cli list
python -m final_project.cli stats
```

## Streamlit 示例

```bash
streamlit run app.py
```

请在 `docs/screenshots/` 中放入至少 2 张截图，并在这里说明每张截图展示了什么。
